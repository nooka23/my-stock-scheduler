# 개인 종목 위키 진행 기록과 인계

- 마지막 갱신: 2026-09-14 20:25 KST (Asia/Seoul)
- 현재 상태: **운영 DB 적용 완료 — 지정 소유자 등록·서버 인증 차단 검증 / 실기기·배포 검증 대기**
- 현재 요청 범위: 계획 구현과 지정 계정 허용. 로컬 Supabase DB 적용 완료, 배포·외장 하드 작업은 아직 수행하지 않음
- 설계 기준: [plan.md](plan.md)
- 참고 자료: [references/README.md](references/README.md), [HTML·CSS 분석](references/namuwiki-source-analysis.md)

## 다음 AI가 먼저 알아야 할 사항

1. 사용자는 2026-09-14에 “계획대로 읽고 실행하라”고 명시했고, `mhoon@ibinvest.co.kr` 계정을 위키 허용 계정으로 지정했다. 코드·마이그레이션은 로컬 Supabase DB에 적용했으며 배포·외장 하드 작업은 아직 수행하지 않았다.
2. 새 사용자 메시지에서 구현을 요청하면 그 최신 요청을 우선해 작업 범위와 이 문서 상태를 갱신한다. 과거의 계획 단계 문구를 이유로 이미 받은 구현 요청을 다시 승인받지 않는다.
3. 사용자는 본인 한 명이다. 다른 일반 사용자뿐 아니라 다른 웹사이트 관리자도 위키를 사용하거나 읽을 수 없어야 한다.
4. 갤럭시 S25 우선, PC는 1920×1080 디스플레이 기준이다. 나무위키를 대시보드나 카드 중심 디자인으로 재해석하지 않는다.
5. 기존 프로젝트와 운영 인프라를 재사용한다. 기능 구현을 시작하기 전 환경의 현재 상태는 재확인한다.
6. 외장 하드는 사용자가 나중에 마련한다. 로컬 백업과 외부 장치 복구본 확보를 구분하며, 외장 하드 때문에 독립적인 구현·시범 사용을 멈추지 않는다.
7. 저장소에는 위키 작업과 무관한 기존 미커밋 변경·추가·삭제가 많다. 정리하거나 되돌리지 않는다.
8. 사용자가 스크린샷 단독 재현의 한계를 지적한 뒤 `.webarchive`를 제공했다. HTML·CSS 분석은 완료했고 계획 1.2에 반영했다. 같은 파일을 다시 요청하지 않는다. CSS 선언과 실제 브라우저 계산·동작 검증은 구분한다.
9. 전체 원본은 `references/source/`에 로컬 보존·Git 제외. 다른 AI가 참고할 분석 보고서·선별 CSS 근거는 버전 관리 대상이다.

## 확정 조건과 추천안 구분

| 구분 | 내용 |
|---|---|
| 사용자 확정 | 기존 사이트 안에 추가, 본인만 접근, S25 우선, PC 1920×1080, 나무위키 화면에 충실, 자유 본문, 외장 하드 추후 |
| 유지할 요구사항 | 검색·내부 링크·목차·모바일 수정·표/이미지/출처·이력/복구·서버 저장·백업/복원·내보내기 |
| AI 추천안 | `/wiki`, Tiptap+JSON, 첫 버전 절 편집, 고유 문서/제목 ID, 버전 검사, 인증된 첨부 다운로드 |
| 소스 확인 | 본문15px·line-height1.5, 바깥 컨테이너 최대1400px, 반응형 여백·제목·목차·표 선언 |
| 잠정값 | 실제 기기 글꼴·사용성 보정, 하루 1회 로컬 백업, 로컬 7일·외장 일별 30개/주별 12개 보관 |
| 첫 버전 제외 추천 | 다른 사용자 제공·공개 공유·공동 편집·자동 병합·전체 볼트 이전·양방향 동기화·AI 정리 |

## 현재 완료한 일

- [x] 앱 구조·잠금 파일 버전·라우팅·인증·공통 UI·Storage·운영 문서 조사
- [x] 최초 구현 계획을 대화에 작성
- [x] 사용자 제공 PC 2장·S25 1장 확인, 모바일 파일 축소 한계 기록
- [x] 본인 전용·S25 우선·외장 하드 추후 준비 결정을 반영해 계획 수정
- [x] 계획서를 `docs/wiki/plan.md`로 통합
- [x] 이 진행 기록과 문서 진입점을 작성
- [x] 참고 이미지 원본 복사 및 체크섬·문서 링크 검증
- [x] 루트 `AGENTS.md` 안내 연결 검증
- [x] 스크린샷 단독 재현의 한계와 사용자 제공 HTML·CSS 저장본 분석 절차를 계획에 반영
- [x] 웹 아카이브 수신·원본 보존·외부 CSS 3개 및 인라인 스타일 3개 정적 분석
- [x] HTML·CSS 분석 보고서·선별 선언 87개 작성, 계획 1.2 수치 보정
- [x] `/wiki` 읽기·검색·새 문서·전체/절 편집·이력·복원·내보내기 UI와 Tiptap 편집기 작성
- [x] 위키 전용 DB·RLS·비공개 Storage·인증 첨부 다운로드 마이그레이션 작성

## 구현 단계 상태

| ID | 단계 | 상태 | 완료 증거 |
|---|---|---|---|
| W00 | HTML·CSS 원본으로 디자인 근거 보강 | 완료 — 정적 분석 범위 | [분석 보고서](references/namuwiki-source-analysis.md), [CSS 근거](references/style-evidence.json), 원본 체크섬 일치·연결 CSS 3개 포함·선언 추출. 실제 동작 검증과 구분 |
| W01 | 읽기 화면 재현·편집기 초기 시험 | 진행 중 | 나무위키 CSS 수치·Tiptap 읽기/편집 UI 작성, 대상 ESLint·production build 통과. 실제 S25 대조 미실행 |
| W02 | 본인 전용 인증·레이아웃·DB·Storage | 검증 중 | 마이그레이션·RLS·비공개 bucket·첨부 API를 로컬 Supabase DB에 적용, 지정 소유자 UUID 등록. 권한 함수 owner=true/other=false 및 비로그인 첨부 API=401 확인, 실제 본인 브라우저 업로드 시험 대기 |
| W03 | S25 → 서버 저장 → PC 확인 | 미착수 | 저장 RPC·기기별 초안 코드 작성. 실제 서버·기기 확인 없음 |
| W04 | 편집·검색·링크·목차·표·이미지·각주 | 진행 중 | 전체/절 편집·검색·목차·표·이미지·이력 UI 작성. 내부 링크 색인·실사용 검증 보강 필요 |
| W05 | 초안·충돌·이력·복구 | 진행 중 | base revision 충돌 처리·mutation ID·초안·이력/복원 코드 작성. 통신 단절·다중 기기 시험 없음 |
| W06 | 내보내기·로컬 백업·격리 복원 | 진행 중 | JSON·Markdown·이력·첨부 ZIP 내보내기 작성. 서버 백업 연결·격리 복원 미실행 |
| W07 | 실기기·이동통신망·Preview·기존 화면 검증 | 미착수 | 검증 미실행 |
| W08 | 외장 하드 연동·복원 | 외장 하드 준비 후 | 사용자가 추후 준비 예정. W01~W07의 선행 조건이 아님 |

권장 상태 값: `미착수`, `진행 중`, `검증 중`, `완료`, `보류`. `완료`에는 실제 산출물과 검증 증거가 필요하다. 부분 구현은 완료로 올리지 않는다.

## 다음 작업

코드 구현은 시작됐고 운영 적용 전 단계다. 기존 무관한 변경을 건드리지 않는다.

다음 작업은 S25와 PC에서 첫 문서를 만들고 저장·검색·충돌·첨부·복원을 실제로 검증하는 것이다. 운영 적용 이력과 확인 항목은 [operations.md](operations.md)에 있다.

구현 요청을 받았을 때의 첫 작업 후보:

1. 최신 사용자 요청·루트 AGENTS·이 문서·계획·참고 이미지를 읽는다.
2. 기존 미커밋 변경을 확인하고 위키 변경 범위를 분리한다.
3. W00 분석 보고서와 선별 CSS를 읽고 계획 1.2의 값으로 W01 읽기 화면과 S25용 편집기를 시험한다. 실제 기기 viewport·폰트·줄바꿈을 기록한다. 실제 데이터 연결 전에는 샘플 문서로 검증 가능하다.
4. W02에서 본인 허용 UUID·기존 세션 처리·실제 DB/Storage 정책을 확인한다. 메뉴 숨김만으로 접근 제한을 구현했다고 판단하지 않는다.
5. W03의 휴대폰 수정·서버 저장·PC 확인 흐름을 먼저 완결한다.

진행을 위해 지금 사용자에게 다시 물을 큰 방향은 없다. 위키 이름은 임시값 사용 가능하고 실제 기기의 브라우저·viewport와 외장 하드 경로는 해당 단계에서 확인한다.

## 미검증·주의 사항

- 나무위키 직접 브라우징은 도구의 사이트 접근 정책으로 차단되어 우회하지 않았다. 현재 근거는 사용자 제공 스크린샷과 HTML·CSS 저장본의 정적 분석이다.
- 모바일 이미지는 163px 폭으로 축소되어 실제 viewport·터치 영역을 측정할 수 없다. CSS의 기본 글자 크기는 저장본에서 확인했다.
- 글꼴 선언은 확인했지만 실제 선택된 폰트는 미확인이다. 목차 details·각주 앵커·절 접기 상태 CSS·편집 링크는 확인했지만 실제 조작·편집 페이지는 검증하지 않았다.
- Docker 소켓 읽기 권한 제한으로 실행 중인 컨테이너를 직접 확인하지 못했다.
- Vercel 현재 배포 환경변수·운영 DB 정책·최근 자동 백업·전체 복원 성공은 미검증이다.
- 기존 루트 스크롤 제한, `/`·`/login`만 대상으로 하는 미들웨어, 공개 `stocks` 버킷을 위키 보호 기능으로 간주하지 않는다.
- 현재 빌드 설정은 타입 오류를 무시할 수 있으므로 빌드 성공과 타입 검증 성공을 구분한다.
- 서비스 역할 키·DB 관리자 권한은 일반 사용자 접근 테스트의 대체 수단이 아니다.
- 단기 서명 URL은 최초 제안이었으나 본인 전용 조건 반영 후 기본안에서 제외했다. 최신 설계는 인증된 파일 다운로드다.
- 마이그레이션은 로컬 Supabase DB에 적용했고 `wiki_access`에 지정 계정 하나를 등록했다. production Vercel 배포와 외부 접근은 별도 검증이 필요하다.

## 검증 기록

| 날짜 | 대상 | 결과 | 해석 |
|---|---|---|---|
| 2026-09-14 | 저장소·운영 문서 읽기 | 조사 완료 | 파일·기록의 확인이며 운영 전체 기능 검증 아님 |
| 2026-09-14 | 사용자 제공 이미지 | 3장 직접 열람·크기 확인 | 데스크탑 배치와 모바일 순서 확인, 원본 CSS와 동작은 별도 |
| 2026-09-14 10:29 KST | 문서·이미지 보존·AGENTS 링크 | 통과 | Markdown 5개 문서의 로컬 링크 28개와 공백 검사 통과, 이미지 3장 SHA-256 원본 일치, `git diff --check` 통과 |
| 2026-09-14 | lint·build·앱 테스트·DB 쓰기·배포 | 실행하지 않음 | 문서화 요청이며 앱 구현 없음 |
| 2026-09-14 | Safari 페이지 저장 방법 | 공식 안내 확인 | Apple 지원 문서의 파일 → 별도 저장 → 웹 아카이브 방식 확인. 나무위키 저장 성공·자원 포함 여부는 미검증 |
| 2026-09-14 | 디자인 검증 방식 보완 문서 | 통과 | Markdown 5개 문서의 로컬 링크 30개·공백·최종 줄바꿈 확인, `git diff --check` 통과 |
| 2026-09-14 13:02 KST | 사용자 제공 웹 아카이브 분석 | 정적 분석 완료 | 원본/보존본 SHA-256 일치, 연결 CSS 3개 포함, 외부·인라인 CSS 6개 파싱, 선별 규칙 87개 기록. 실제 렌더링·동작 검증 아님 |
| 2026-09-14 13:05 KST | 최종 문서·원본 검증 | 통과 | Markdown 6개·로컬 링크 43개·공백·최종 줄바꿈, 원본/복사본 및 CSS 체크섬, 상대 URL 정규화 후 연결 CSS 3개 포함, 선별 선언 87개 원본 일치, Git 원본 제외, `git diff --check` 확인 |
| 2026-09-14 14:10 KST | 위키 구현 코드 | 통과 | 위키·공통 메뉴·미들웨어 대상 ESLint 통과, 위키 관련 TypeScript 오류 없음, 네트워크 허용 환경의 `npm run build` 통과. 운영 DB·S25·권한 계정 시험은 미실행 |
| 2026-09-14 14:25 KST | 로컬 Supabase 위키 마이그레이션·소유자 | 통과 | 테이블·RLS·함수·비공개 bucket 적용. 지정 계정 UUID 등록 확인. `authenticated` 역할+JWT claim 모의 읽기 전용 트랜잭션에서 owner=true, 임의 UUID=false 확인 |

## 작업 로그

최신 로그를 아래에 이어 붙인다. 위의 현재 상태와 다음 작업도 함께 갱신해 로그 전체를 읽지 않아도 인계가 가능하게 한다.

### 2026-09-14 — 조사·최초 계획·사용자 피드백 반영

- 사용자 요청: 기존 웹사이트에 개인 종목 위키를 추가할 계획 수립. 코드는 수정하지 않음.
- 수행: 프로젝트·운영 문서 조사, 편집기·저장·권한·이력·백업·내보내기 설계.
- 디자인: 직접 사이트 접근은 차단됨. 이후 사용자가 제공한 동일 문서 PC 2장·S25 1장을 확인해 계획 수정.
- 사용자 결정: 본인만 사용, 다른 이용자는 사용/열람 불가, S25 우선, PC 1920×1080, 외장 하드 추후 준비.
- 설계 변경: 단순 사용자별 소유 정책에 허용 UUID 제한을 추가. 서명 URL 기본안 대신 인증된 첨부 다운로드. 외장 하드 전후 백업 단계 분리.
- 결과: 대화의 계획만 존재했고 기능 구현은 시작하지 않음.

### 2026-09-14 — 계획서·진행 기록 문서화

- 사용자 요청: 계획이 파일로 없으면 만들고 모든 AI가 참고할 진행 기록도 작성.
- 사전 확인: 저장소의 docs·README·AGENTS에서 기존 위키 계획 문서를 찾지 못함.
- 산출물: `docs/wiki/README.md`, `plan.md`, `progress.md`, `references/README.md`와 원본 이미지 3장, 루트 `AGENTS.md` 안내.
- 변경 범위: 문서·참고 이미지·AI 작업 안내만. 앱 코드·패키지·DB·운영 설정·배포는 변경하지 않음.
- 검증: Markdown 5개 문서의 로컬 링크 28개·공백·최종 줄바꿈 확인. 이미지 3장 원본 SHA-256 일치. `git diff --check` 통과. 이번 변경은 루트 `AGENTS.md`의 안내 9줄과 새 `docs/wiki/` 문서·이미지에 한정됨.
- 다음: 기능 구현을 시작하려면 최신 사용자 요청에서 작업 범위가 바뀌었는지 확인.

### 2026-09-14 10:45 KST — HTML·CSS 자료 기반 디자인 검증 단계 추가

- 사용자 요청: 스크린샷 단독 구현의 불완전성을 지적하고 직접 접속 또는 소스 제공 방식을 검토.
- 판단: 직접 접근은 현재 브라우저 도구 정책으로 차단되어 재시도·우회하지 않음. 사용자가 제공한 로컬 페이지 저장본을 분석하는 방식을 추천.
- 변경 파일: `plan.md` 버전 1.1, `progress.md`, `references/README.md`.
- 설계 변경: W00 자료 확인 단계 추가. 기존 디자인 수치는 잠정 유지. HTML·CSS 분석과 동적 동작·S25 확인을 구분.
- 검증: Apple 공식 저장 안내 확인. Markdown 5개 문서의 로컬 링크 30개·공백·최종 줄바꿈 확인과 `git diff --check` 통과. 원본 저장본 수신·CSS 분석·앱 구현은 수행하지 않음.
- 다음: 사용자 제공 `.webarchive` 또는 HTML·리소스 ZIP의 포함 자원을 검사하고 필요한 자료만 추가 요청.

### 2026-09-14 13:02 KST — 사용자 제공 웹 아카이브 정적 분석

- 사용자 입력: 요청했던 Safari `세이디 싱크 - 나무위키.webarchive` 제공. 이전의 계획·문서화 범위 유지.
- 관련 단계: W00, 자료 미수신 → 정적 분석 완료. W01~W07 기능 구현은 미착수 유지.
- 수행: plistlib로 주 문서·자원 확인, HTML 구조와 PostCSS 선언·미디어 쿼리 분석. 원본 스크립트 실행·원격 요청 없음.
- 산출물: `references/namuwiki-source-analysis.md`, `references/style-evidence.json`, 로컬 원본·비실행 확장자 추출본, `references/.gitignore`. 계획·진행 기록·자료 목록·AI 진입 안내 갱신.
- 설계 변경: 계획 1.2. 바깥 폭1400/안쪽1368, 본문15px·줄 간격1.5, 모바일 제목2rem, 문서 반경6px, 목차 내용에 맞는 폭, 1023.98/727.98/420px 조건 등을 원본 근거로 보정.
- 구분: 모바일 보조 목록의 메뉴 이동과 확대·포커스 허용은 자체 사용성 제안. PC CSS 선언을 실제 S25 동작 확인으로 기록하지 않음.
- 보존: 전체 원본은 로컬 `source/`에 유지하고 Git 제외. 핵심 판단과 선별 CSS 근거는 원본 없이도 읽을 수 있도록 별도 기록.
- 검증: 원본과 보존본 체크섬, 연결 CSS 포함 여부, 6개 스타일 구문 분석과 선별 선언 일치 확인. 상대·절대 URL 비교를 정규화해 재검사했고 통과. 문서 6개·로컬 링크 43개·공백·최종 줄바꿈과 `git diff --check` 통과.
- 미실행: 앱 코드·패키지 변경, DB 쓰기·배포, lint/build, 실제 사이트·S25 조작.
- 다음: 사용자 후속 구현 요청 시 W01. 같은 저장본을 다시 요청하지 않으며 실제 폰트·viewport·각주/접기 동작은 해당 단계에서 확인.

### 2026-09-14 14:10 KST — 계획 기반 위키 구현 시작

- 사용자 요청: “계획대로 읽고 실행하라.”
- 관련 단계: W01·W02·W04·W05·W06 진행 중. W03·W07·W08은 실제 환경 검증 전 미착수.
- 구현: `/wiki` 전용 고정 스크롤 프레임, 나무위키 원본 CSS 기준 읽기 UI, Tiptap JSON 편집기, 전체·절 편집, 검색, 목차, 표·이미지, 이력·복원, ZIP 내보내기를 작성.
- 보안: `wiki_access` 단일 허용 UUID, RLS, 저장 RPC의 버전 검사·mutation ID, 비공개 `wiki-private` bucket, 인증된 첨부 다운로드 API, 로그인 미들웨어와 소유자 전용 메뉴를 추가.
- 파일: `supabase/migrations/20260914000000_create_personal_wiki.sql`, `src/app/wiki/`, `src/components/wiki/`, `src/lib/wiki/`, 첨부 API, `Sidebar.tsx`, `middleware.ts`, `operations.md`, 의존성 잠금 파일.
- 검증: 위키 대상 ESLint 통과. 위키 대상 TypeScript 오류 없음. 네트워크 허용 환경에서 production build 통과. 기존 전역 `tsc`에는 변경 전부터 있던 차트·next.config 타입 오류가 남아 있으며 위키 오류와 구분했다.
- 미실행: 마이그레이션 적용, 본인 UUID 등록, 운영 DB/Storage 쓰기, 배포, S25·PC·다른 계정·이동통신망·통신 단절·격리 복원 시험.
- 다음: [operations.md](operations.md)의 마이그레이션과 본인 UUID 등록을 적용한 뒤, W03·W07 인수 시나리오를 실제 기기에서 완료한다.

### 2026-09-14 14:25 KST — 지정 소유자 등록과 DB 적용

- 사용자 입력: `mhoon@ibinvest.co.kr` 계정을 유일한 위키 허용 계정으로 지정.
- 수행: 서비스 역할로 계정 존재·승인 상태를 확인하고 Auth UUID를 조회. 실행 중인 `supabase-db` 컨테이너에 위키 마이그레이션을 적용하고 `wiki_access` 단일 행을 등록.
- 검증: 테이블·비공개 bucket 생성, 등록 UUID 일치 확인. `authenticated` 역할과 JWT subject를 모의한 읽기 전용 트랜잭션에서 지정 UUID는 `wiki_is_owner()=true`, 임의 UUID는 `false`.
- 미실행: 브라우저 로그인·S25·다른 실제 계정·첨부 업로드·저장/복원/내보내기·Vercel 배포. 본인 UUID 값과 환경 비밀은 이 문서에 기록하지 않음.
- 다음: 실제 브라우저에서 `/wiki`로 첫 문서를 만들고 PC/S25 간 저장 흐름을 검증.

### 2026-09-14 20:25 KST — 첨부 인증 경로 런타임 점검

- 관련 단계: W01·W02
- 수행: Next.js 16의 비동기 `cookies()` API에 맞춰 위키 첨부 다운로드 route의 Supabase cookie accessor를 수정했다.
- 검증: 위키 대상 ESLint와 production build 통과. 실행 중인 로컬 개발 서버에서 비로그인 `GET /api/wiki/attachments/<임의 UUID>`가 `401 Unauthorized`를 반환하는 것을 확인했다. `git diff --check`도 통과했다.
- 미실행: 지정 계정 로그인 후 실제 파일 업로드·다운로드, S25/PC 저장 흐름, 다른 실제 계정 차단, 배포 환경 검증.
- 다음: 실제 브라우저에서 `/wiki`로 첫 문서를 만들고 PC/S25 간 저장 흐름을 검증.

## 이후 작업 기록 양식

아래 양식을 복사해 작성한다. 추정이나 계획을 실제 수행으로 채우지 않는다.

```markdown
### YYYY-MM-DD HH:mm KST — 작업명

- 사용자 요청과 이번 작업 범위:
- 관련 단계: Wxx
- 시작 상태 / 종료 상태:
- 수행한 내용과 변경 파일:
- 사용자 결정 / 설계 변경과 이유:
- 검증: 명령 또는 실기기 시나리오, 결과, 증거 경로
- 수행하지 못한 검증 / 실패 / 남은 위험:
- 다음 작업과 필요한 입력:
```

작업 종료 시 확인:

- 현재 상태·단계표·다음 작업이 실제 상태와 일치하는가?
- 검증을 하지 않았다면 그 사실을 적었는가?
- 사용자 확정과 추천·잠정값을 구분했는가?
- 변경된 요구사항을 계획서에도 반영했는가?
- 기존 무관한 변경을 보존했는가?
- 비밀정보 없이 다음 AI가 이어갈 수 있는가?
